<div class="row center">
    <h1>Profile de <%= @user.username %></h1>

    <%= gravatar_for @user, size: 120  %>

    <h3 >Articles de <%= @user.username %></h3>
    <div class="col-md-6 col-md-offset-3"> 
        <%= render 'articles/articles', obj: @user.articles %>
    </div>
</div>